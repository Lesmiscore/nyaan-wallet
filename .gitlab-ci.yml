image: node

stages:
  - main

main:
  stage: main
  before_script:
    - apt-get update
    - apt-get install -y git
  script:
    - npm i
    - npm run build
    - cd docs/wallet/
  after_script:
    - git init
    - git config user.email "nao20010128@gmail.com"
    - git config user.name "nao20010128nao"
    - git checkout --orphan gh-pages
    - git add .
    - git commit -m 'Commit'
    - git remote add origin https://nao20010128nao:$TOKEN@github.com/nao20010128nao/nyaan-wallet.git
    - git push -f -u origin gh-pages
